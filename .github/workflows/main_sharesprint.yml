name: Build and deploy Python app to Azure Web App - sharesprint

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ShareSprint is deployed manually via Azure Kudu. This workflow is kept only for reference."
